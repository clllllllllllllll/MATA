[pytest]
markers =
    migration_mutation: serial direct-owner tests that mutate the exact disposable database migration state
    post_boundary_migration: lifecycle tests confined to the irreversible 000040-to-head migration segment
